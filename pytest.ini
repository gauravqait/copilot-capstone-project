[pytest]
testpaths = tests
addopts = --tb=short -v
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    integration: end-to-end integration tests requiring the full project structure
    unit: unit tests for individual functions and modules
